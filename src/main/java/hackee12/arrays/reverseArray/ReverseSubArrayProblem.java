package hackee12.arrays.reverseArray;

public interface ReverseSubArrayProblem {

    /**
     * Reverse subArray in-place.
     * @param nums array where subArray needs to be reversed in-place
     * @param from inclusive from index
     * @param to exclusive to index
     */
    void reverse(int[] nums, int from, int to);
}
