package hackee12.arrays.findSingleNumber;

/**
 * In a non-empty array of integers every number appears twice, except one.
 * Find that single one.
 */
public interface FindSingleNumberProblem {
    int singleNumber(int[] nums);
}
