package hackee12.arrays.runningSum;

/**
 * Return a running sum of an array as runningSum[i] = nums[0] + nums[1] + .. + nums[i].
 */
public interface RunningSum1D_Problem {
    int[] runningSum(int[] nums);
}
